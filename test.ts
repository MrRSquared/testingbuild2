// tests go here; this will not be compiled when this package is used as an extension.
basic.showNumber(custom.fib((4))) //This would break if we used a string instead of a number. That is how the Unit Tests work.
